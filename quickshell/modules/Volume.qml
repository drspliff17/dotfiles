import QtQuick

Rectangle {
    id: root

    Text {
        id: textItem

        text: "Thing"
        color: Colors.colors.color5

        horizontalAlignment: Text.AlignHCenter

        anchors.centerIn: parent
    }
}
